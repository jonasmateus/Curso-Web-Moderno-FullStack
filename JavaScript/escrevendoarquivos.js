const fs = require('fs')

let arquivo = {
    nome : 'Alisson',
    idade : '23',
    salario : '2.340'
}

//fs.writeFile(__dirname + "/arquivoGerado.json", JSON.stringify(arquivo),
 //(erro) => console.log(erro || 'Arquivo escrito corretamente !'))
 


